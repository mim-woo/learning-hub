# 데이터 처리           

> ADsP 3과목 학습 내용에 기초로 하나 통계학입문, 산업공학기초통계, 데이터분석 등의 전공과목에서 학습 및 사용한 내용을 추가함.

> 도서 Hands-on-ML 독학에 따라 내용 및 코드를 추가해갈 예정임.

> 내용에 더해 각 처리방법 및 과정이 실제 분석에 주는 영향을 중점적으로 정리

## ?. 결측값 처리

- 결측값 처리에서의 고려 사항 : 1). 효율성 2). 자료처리 및 분석 복잡성 3). 관측값과 결측값 사이 편이(Bias)

**1). 결측값 대치**

- 단순 대치법 : 모든 결측값을 하나의 값으로 대체 / 간편하나 왜곡이 발생하기 쉬움

  (1). 완전 분석법 : 결측치가 포함된 자료를 제외, 완전한 관측치만을 대상으로 통계 기법을 적용 / 분석이 쉬워지나 부분적인 관료를 무시하므로 효율성과 타당성의 문제 발생
  
  (2). 평균 대치법 : 관측된 자료의 적절한 평균값으로 결측값들을 대치해 보완된 자료를 측정된 자료로 간주 / 완전 분석법보다 효율성을 향상 / 통계량의 표준오차를 과소 추정하게 됨 
  
  (3). 단순 확률 대치법 : 평균 대치법의 표준오차 과소 추정문제를 보완, Hot-deck 방법, Nearest Neighbour 방법 등 존재 / 관측된 자료로 추정한 통계량으로 결측값에 확률값을 부여해 대치 / 대부분의 경우 추정량의 표준오차 계산 자체가 어려워짐

     Cold-deck : 외부 자료를 사용해 결측값을 대체
     Hot-deck : 동일한 자료 내에서 결측값 대체

     Nearest Neighbour : 정의에 따라 거리가 가까운 자료들을 기준으로 결측값을 대체 

  (4). 회귀 대치법 : 대치하려는 결측치가 있는 변수를 종속 변수로 설정하고 결측치가 없는 변수들을 독립 변수로 사용, 회귀 모델링을 통해 결측값을 예측

- 다중 대치법 : 표준오차 과소추정, 계산의 난해성 문제를 보완 / m번의 단순 대치법을 통해 m개의 가상적 자료를 만들어 분석 / 대치 - 분석 - 결합의 3단계로 구성
  
**2). R에서의 적용 / 결측치 파악을 위한 기초적 통계 포함**

- head() : 앞 6행만 불러옴

- str() : 데이터 구조 파악

- summary() : 기초 통계량 파악 -> 4분위값, 중앙값 등을 구할 수 있으므로 비대칭도 등을 구하는데 활용 가능 / 범주형 데이터는 빈도수 출력


- R에서 사용하는 결측값 처리 관련 패키지는 Amelia, Mice, mistools 등

  is.na() : NA를 논리값으로 반환 (NA=TRUE)
  
  complete.cases() : NA를 논리값으로 반환 (NA=FALSE)
   

> 결측값은 흔한 현상이며 특히 학습용 데이터셋이 아닌 실제 데이터에서는 주의해야 함.
> 이를 고려하지 않을 시 정렬, 분석, 통계 등에서 오류나 왜곡 문제 발생.
> 문제 상황에 맞는 효율적 처리로 손실과 왜곡, 시간 사이 균형을 찾아야 함.

----------------------------------------------------------------------------------------------------------------------------

## ?+1. 이상값 탐지

- 전처리 방향 결정이나 규칙성 발견에 사용될 수 있음

- 이상값 vs Bad data : 의도되지 않은 현상이나 분석 대상, 의도된 이상값 / 의도치 않게 잘못 기입된 값, 분석 목적에 부합되지 않아 제거해야 하는 값

- ESD 알고리즘(평균과 표준편차를 이용해 이상값을 반복적으로 탐지하고 제거), MADM 등의 알고리즘과 box plot, summary 함수 등을 활용

> 이상값은 통계나 분석을 왜곡할 우려가 있으나, 제거로 인해 데이터의 객관성이나 변동성이 훼손될 수 있음.
> 따라서 이상값 탐지에 너무 많은 시간을 할애하는 것은 부적절.
> 평균과 중위수 값, 제 1사분위수와 제 3사분위수 정도를 활용해 하는 것이 일반적이며 필요 시 시각화를 통해 특성을 파악.
> 단, 부정사용방지 등 일부 프로젝트에서는 이상값 탐지가 주요 과제일 수 있음.

----------------------------------------------------------------------------------------------------------------------------

## ??. R 기초

- 데이터 할당 시 = 이나 <- 을 사용

**1). 데이터 구조**

- vector -> metrix -> array / data frame / list

(1). vector

  - 하나의 스칼라 값이나 하나 이상의 스칼라 원소를 가지는 집합

  - 숫자, 문자, 논리연산자를 원소로 가지며 같은 자료형을 갖는 값들의 집합

  - c(), :, seq(), rep()으로 생성

  - 벡터 간 연산 시 짧은 쪽을 처음부터 재사용, 길이는 배수 관계인 게 원칙이나 아니어도 경고 메시지만 나오고 연산 가능

(2). metrix

  - 행과 열의 수가 지정된 구조

  - 한 가지 유형의 스칼라만 사용

(3). data frame

  - 행렬과 유사한 2차원 구조이나 서로 다른 데이터 타입을 가질 수 있음

  - 벡터들의 길이가 반드시 같아야 함

(4). array

  - 다차원 데이터

  -  단일 형태의 데이터만 사용 가능

(5). list

  - 서로 다른 유형의 데이터 포함 가능

  - (키, 값) 형태의 데이터를 가지는 연관 배열

**2). 기초 활용**

(1). 데이터 불러오기

- read.csv(csv 파일) , read.table(txt 파일, Sep과 Header 옵션이 기본 False), read.excel(xls나 xlsx 파일, readxl 패키지 사용)

(2). 기초 통계

(3). 시각화

- scatter plot : plot(y~x) or plot(x,y)

- scatter metrix : pairs()

- histogram : hist()

- box plot :

- mosaic plot : 

(). 기타 함수

**3). 데이터 마트 구축**

(1). reshape 패키지

- 데이터를 재정렬 / 원래 데이터의 모든 정보를 그대로 유지하는 것이 특징

- melt()를 이용해 여러 변수로 구성된 데이터를 재구성 : id(식별에 사용되는 변수들, 각각의 칼럼 유지), variable(사라진 칼럼명), value(사라진 칼럼에 있던 값) 형태

- cast()를 이용해 melt()가 적용된 데이터를 여러 칼럼으로 변환 : 각각의 변수를 지정한 변수값을 행과 열로 삼아 재정렬

(2). sqldf 패키지

- sql을 기반으로 데이터를 핸들링할 수 있도록 함

- sqldf("sql 구문") 형태

(3). plyr 패키지

- 데이터 분할 / 분할된 데이터에 함수 적용 /  결과를 재결합

- for 문 없이도 빠르게 함수를 처리

- apply(x, margin, fun) 형태 : margin=1이면 행 방향, 2이면 열방향

(4). 데이터 테이블

- 데이터 프레임과 유사하나 그룸화와 순서화가 빨라 분석에서 빠른 연산 속도

- 데이터 프레임과 달리 행번호가 :으로 표
----------------------------------------------------------------------------------------------------------------------------

## 추후 추가 및 학습점

- 이상값 탐지 알고리즘

- 코드 추가

- 데이터 전처리 문서로 둘 것인지, r 기초를 포함할 것인지 결정. / 기타 유용한 함수들 추가 / 시각화 함수 보완
